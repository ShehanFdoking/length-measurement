export function Footer() {
  return (
    <footer className="border-t border-line/70 bg-paper">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 py-6 text-sm text-ink/55 sm:px-6 lg:px-8 md:flex-row md:items-center md:justify-between">
        <p>Length Lab keeps the measurement workflow in one place.</p>
        <p>Frontend: Next.js + Tailwind. Backend: FastAPI.</p>
      </div>
    </footer>
  );
}
